# Ninja Timer -- Project Configuration

## Repository Identity

- **Project**: Ninja Timer (Ninja Israel obstacle course competition timer)
- **GitHub Repo**: `RazTauber/Ninja_Timer`
- **GitHub Account**: `RazTauber`
- **Git Identity**: Raz Tauber <razt@ctera.com>
- **This is a SEPARATE repository** from the parent `Noams_App` project.

## GitHub Credentials

This project uses the same GitHub account as the parent `Noams_App` project.
The Personal Access Token (PAT) is stored in the parent repo's git config at:

```
c:\Users\razt\.cursor\projects\Noams_App\.git\config
```

The remote URL for this repo follows the same pattern:

```
https://<PAT>@github.com/RazTauber/Ninja_Timer.git
```

To extract the PAT from the parent, read the `[remote "origin"]` url from the parent's `.git/config` file.

## Git Setup (already done during initialization)

```bash
git init
git remote add origin https://<PAT>@github.com/RazTauber/Ninja_Timer.git
```

## Tech Stack

- Vanilla HTML + CSS + JavaScript (ES modules)
- Vite for dev server and bundling
- No framework — intentionally simple
- Excel export (.xlsx) via `xlsx-js-style` library
- localStorage for session persistence (prefixed `ninja_timer_`)
- RTL Hebrew UI throughout
- Dual-site architecture: regular + finals (build-time `VITE_APP_MODE` flag)

## Deployment

- **Platform:** Cloudflare Pages (static Vite build)
- **Regular site:** https://ninja-timer.pages.dev/
- **Finals site:** https://ninja-timer-finals.pages.dev/

```bash
# Regular site
npm run build
npx wrangler pages deploy dist --project-name ninja-timer --branch master

# Finals site
npm run build:finals
npx wrangler pages deploy dist-finals --project-name ninja-timer-finals --branch master
```

## Important Notes

- When pushing or pulling, use the remote URL with the embedded PAT from the parent Noams_App repo.
- Never commit `.env` files or expose the PAT in committed files.
- The `.gitignore` is configured to exclude `node_modules/`, `dist/`, and `.env`.
- All competition data is CONFIDENTIAL — local-only storage, zero network transmission.
- CSP meta tag enforces no external script/connect sources.
